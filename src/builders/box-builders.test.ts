import {describe, expect, it} from 'vitest';

describe('Basic Box builder', () => {
  describe('The constructor', () => {
    it('should be true', () => {
      expect(true).toBe(true);
    });
  });
});
